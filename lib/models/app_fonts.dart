import 'package:flutter/material.dart';
import 'app_colors.dart';

class AppFonts {
  static const TextStyle title = TextStyle(
    fontSize: 28, // Tamanho do título
    fontWeight: FontWeight.bold, // Negrito
    color: AppColors.text, // Padrão para textos
  );

  static const TextStyle titleAppBar = TextStyle(
    fontSize: 28, // Tamanho do título
    fontWeight: FontWeight.bold, // Negrito
    color: AppColors.titleAppBar, // Padrão para títulos
  );

  static const TextStyle texto = TextStyle(
    fontSize: 15, // Tamanho do título
    color: AppColors.text, // Padrão para textos
  );

  static const TextStyle textomini = TextStyle(
    fontSize: 11, // Tamanho do título
    color: AppColors.text, // Padrão para textos
  );

  static const TextStyle textoNegrito = TextStyle(
    fontSize: 16, // Tamanho do título
    fontWeight: FontWeight.bold, // Negrito
    color: AppColors.text, // Padrão para textos
  );

  static const TextStyle textoNegritomini = TextStyle(
    fontSize: 12, // Tamanho do título
    fontWeight: FontWeight.bold, // Negrito
    color: AppColors.text, // Padrão para textos
  );

  static const TextStyle textoRodape = TextStyle(
    fontSize: 12, // Subtítulo ou intermediário
    color: AppColors.textoRodape, // Padrão para rodape
  );
}
